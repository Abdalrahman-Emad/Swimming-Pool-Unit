import dashboard from './dashboard';
import pages from './pages';
import tasks from './utilities';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, pages, tasks ] 
};

export default menuItems;
